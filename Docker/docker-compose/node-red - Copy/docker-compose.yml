version: '3'
services:
  nodered:
    image: "nodered/node-red"
    ports:
      - "1880:1880"
      - "8120:8120"
# docker compose up -d
# docker compose down
# docker compose start
# docker compose stop